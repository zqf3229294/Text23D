# Text23D Frontend

Angular workbench for conversational CAD generation and Three.js preview.

## Local setup

```powershell
cd frontend
npm install
npm start
```

The app expects the backend at `http://localhost:8000` by default. Change
`src/environments/environment.ts` if needed.
